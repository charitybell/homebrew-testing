name: build and release

on:
  push:
    branches:
      - 'main'
      
jobs:
  prepare:
    outputs:
      matrix: ${{ steps.set-output-matrix.outputs.matrix }}
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 2

    - name: Set up Homebrew
      id: set-up-homebrew
      uses: Homebrew/actions/setup-homebrew@master

    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v35
      with:
        files: Formula/*.rb
        json: true

    - name: set output matrix
      id: set-output-matrix
      run: |
        echo "matrix={\"formula\": ${{ steps.changed-files.outputs.all_changed_files }} }" >> $GITHUB_OUTPUT
  build:
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.prepare.outputs.matrix) }}
    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@master
        
      - name: set up homebrew tap
        id: set-up-tap
        run: |
          mkdir -p $(brew --prefix)/Library/Taps/${{ github.actor }}
          echo $GITHUB_WORKSPACE
          ls -l $(brew --prefix)/Library/Taps/${{ github.actor }}

      - name: build bottle (${{ matrix.formula }})
        run: |
          brew install --build-bottle ${{ github.repository }}/$(basename ${{ matrix.formula }})
          brew bottle --write !$
          ls
